export const LinhaDeCards = (props:{temaEvento:[]}) => {

    // function addTema() {

    // }

    // function addCard(){

    // }
    // if(props.temaEvento.length > 0)
    
    // return (

    // )

}